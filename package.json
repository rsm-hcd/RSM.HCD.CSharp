{
  "name": "@rsm-hcd/csharp",
  "version": "3.0.0",
  "description": "Simple nodejs tooling for managing the project",
  "private": true,
  "scripts": {
    "preinstall": "dotnet tool restore && dotnet husky install",
    "test": "dotnet test  -p:Threshold=0",
    "test:coverage": "dotnet test -p:CollectCoverage=true -p:CoverletOutputFormat=opencover -p:Threshold=0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/rsm-hcd/RSM.HCD.CSharp.git"
  },
  "author": "rsm-hcd",
  "license": "Apache-2.0",
  "homepage": "https://github.com/rsm-hcd/RSM.HCD.CSharp#readme",
  "devDependencies": {
    "prettier": "^3.2.5"
  },
  "packageManager": "pnpm@9.1.2",
  "engines": {
    "node": ">=20"
  }
}
